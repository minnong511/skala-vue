.vscode - 에디터 개인 설정 폴더 
node_modules - 외부 라이브러리 
public - 순수 정적 자원(Static Assets) 저장소, 컴파일하지 않는다. 
src - 실제 소스코드 작업 공간
.gitattributes - 서로 다른 OS를 쓰는 개발자들이 협업할 때 사용 
.gitignore - 원격 서버에 업로드되지 않는 파일들을 명세 
index.html - 브라우저가 최초로 보여주는 단 하나의 진짜 HTML 파일 
package.json - npm의 핵심 명세서 
README.md - 프로젝트 가이드 문서 
vite.config.js - 빌드 도구인 Vite의 전역 환경 설정 
------ 

Vue는 한마디로 데이터가 바뀌면 화면도 자동으로 바꿔주는 도구 

1. 브라우저 접속 
2. index.html의 <div id = "app"> 
3. main.js 가 Vue 실행 
4. APP.vue 표시 
5. Router가 URL에 맞는 화면 표시 
6. 화면 출력 

------ 홈페이지 접속 예제

1. http://localhost:5173/
2. Vite 개발 서버 
3. index.html의 <div id="app">
4. main.js가 Vue 앱 시작
5. App.vue의 <RouterView />
6. router/index.js가 "/" 경로 확인
7. HomeView.vue 또는 AboutView.vue 표시

쉽게 말하자면.

주소창에 홈페이지 주소를 입력합니다.
Vite가 index.html을 불러옵니다.
main.js가 Vue를 실행합니다.
App.vue가 화면을 담을 자리를 만듭니다.
Router가 현재 주소 /를 확인합니다.
/에 연결된 Vue 화면을 <RouterView /> 안에 보여줍니다.
해당 화면의 내용이 브라우저에 나타납니다.